"""
Usage of operators
=  Assignment (To assigning values to variables)
// float (IF we need to display the disk usage of 2.34GB as 2GB without decimal values)
%  Modulas (To get reminder)
+= Addition assignment (a=3; a=a+1; a+=1)
-= Substraction assignment
== Relational
<  Less than
<  Greater than

-----Identity-----
is (To compare two EC2 Instances by its RAM, CPU etc | a=2 b=2; a is b; true)
is not (To compare two EC2 Instances by its RAM, CPU etc | a=2 b=2; a is b; false)

-----Logical-----
AND (If we required an EC2 Instance that meets free tier AND 2GB RAM)
OR
NOT
"""